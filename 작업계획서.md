# RPS(가위바위보) 손 제스처 인식 프로젝트 — 작업계획서

작성일: 2026-08-19
저장소: https://github.com/BG-traveling/RPS-Project (main 브랜치)
스코프: 2일 미니 프로젝트

---

## 1. 프로젝트 목표

웹캠 입력으로 실시간 가위/바위/보 손 모양을 인식하고, 인식 결과에 따라 특정 동작(액션)을 트리거하는 딥러닝 시스템 구축. CNN 전이학습(MobileNetV2 + ImageNet 사전학습 가중치)을 사용하며, 학습(오프라인 1회)과 추론(실시간 웹캠)은 서로 직접 호출하지 않고 저장된 모델 파일(`models/*.keras`)로만 연결되는 구조로 설계.

## 2. 데이터셋

| 항목 | 내용 |
|---|---|
| 출처 | Kaggle `rps-cv-images` (Julien de la Bruère-Terreault, CC-BY-SA 4.0) |
| 원본 저장소 | https://github.com/DrGFreeman/rps-cv |
| 구성 | 총 2,188장 — rock 726 / paper 712 / scissors 750 |
| 규격 | 300×200, RGB, PNG |
| 핵심 리스크 | 전 이미지가 초록 단색(크로마키) 배경 — 모델이 손 모양이 아닌 배경색에 과적합될 위험 (EDA에서 RGB 채널 분석으로 최초 발견) |

## 3. 전체 작업 프로세스 (10단계)

```
1. ✅ 데이터셋 구조 파악
2. ✅ EDA (탐색적 데이터 분석)
3. ✅ train/val/test 분리 (stratified, 70:15:15)
4. ✅ tf.data 파이프라인 구축 + 증강 (원본 초록 배경 기준)
5. ✅ 배경 합성 (초록 배경 → 다양한 실사 배경 교체)
6. ✅ CNN 전이학습 모델 구축 (MobileNetV2)
7. ✅ 모델 학습 및 평가 (confusion matrix 등) — val 0.951 / test_synthetic 0.936
8. ✅ 실시간 웹캠 추론 파이프라인 구현
9. ✅ 예측 스무딩 + 쿨다운 + 제스처-액션 매핑
10. 🔄 실전 웹캠 테스트 및 필요시 fine-tuning  ← 현재 위치 (직접 촬영 필요)
```

---

## 4. 완료 단계 요약

### 4-1. EDA (`01_eda.ipynb`)
- 클래스별 이미지 개수 확인 (균형 분포 확인)
- 이미지 크기/포맷/손상 파일 점검 (300×200 RGB PNG 통일, 손상 파일 0건)
- 클래스별 샘플 시각화, 밝기 분포 히스토그램
- RGB 채널 분석 → G채널이 R/B보다 확연히 높음 확인 → 배경 편향 리스크 최초 발견

### 4-2. 데이터 분리 및 파이프라인 (`02_data_pipeline.ipynb`)
- `train_test_split` 2단계 + `stratify`로 70/15/15 계층 분리
- `tf.data.Dataset`: `read_file → decode_png → resize(224,224) → /255.0 → shuffle(train만) → batch(32) → prefetch`
- 증강(train 전용): `RandomFlip`, `RandomRotation(0.1)`, `RandomZoom(0.1)`, `RandomBrightness(0.2, value_range=(0,1))`, `RandomContrast(0.2)`
- 트러블슈팅: `RandomBrightness` 기본 픽셀 범위(0~255) 가정 문제 → `value_range` 명시 + `tf.clip_by_value`로 해결

---

## 5. 현재 진행 중 — 배경 합성 (`03_background_synthesis.ipynb`)

### 5-1. 배경 이미지 수집
- Unsplash API, `.env`에 `UNSPLASH_ACCESS_KEY` 보관(코드 노출 방지)
- 키워드 10종(room interior, living room, office desk, outdoor park, kitchen, bedroom, wall texture, street background, cafe interior, garden)으로 다양성 확보
- 현재 50/100장 확보 (시간당 50 요청 제한으로 중단) → **50장으로 우선 진행 결정**

### 5-2. 데이터 리크 방지 원칙 (중요)
`02_data_pipeline.ipynb`에서 이미 나눈 train/val/test split을 그대로 유지한 채, **그 안에서만** 배경 합성을 수행한다. 전체 2,188장을 먼저 합성한 뒤 다시 나누면, 같은 손 인스턴스의 초록 배경/합성 배경 버전이 train과 test에 각각 흩어져 test 정확도가 부풀려지는 데이터 누수가 발생한다.

### 5-3. train/val/test 구성 방식

| 세트 | 구성 | 비고 |
|---|---|---|
| train | 원본 train(약 1,530장) × 배경 2개/이미지 = 약 3,060장 합성 | 배경 50장 풀에서 매번 랜덤 추출, 이미지당 다른 배경 2개 조합 |
| val | 원본 val 이미지 × 배경 1개/이미지 합성 | train과 동일 분포로 맞춰 조기종료 판단 신뢰성 확보 |
| test | ① 원본(초록 배경) 그대로 ② 합성 배경 버전 둘 다 보유 | 배경 증강 효과를 정량 비교하기 위해 두 버전 모두 유지 |
| 자체 웹캠 검증셋 | 30~50장 직접 촬영 (일반 배경) | 최종 실전 일반화 성능의 유일한 신뢰 지표. test 정확도가 높아도 이 수치가 낮으면 여전히 배경 의존 |

### 5-4. 크로마키 합성 구현 절차
1. OpenCV로 이미지를 HSV 변환
2. `cv2.inRange(hsv, lower_green, upper_green)`로 초록 마스크 생성
3. `cv2.morphologyEx`(열림/닫힘)로 마스크 노이즈 제거
4. 마스크 반전 → 손 영역만 추출 → 배경 이미지(300×200 리사이즈)와 합성
5. **HSV 임계값은 데이터셋마다 다르므로 샘플 이미지로 직접 시각화하며 튜닝 필요** (이 단계가 이 노트북의 핵심 난이도)
6. 합성 결과 3×5 grid로 샘플 시각화 → 손 경계 부자연스러운 절단, 배경 겹침 여부 육안 확인
7. `data_synthetic/train/`, `data_synthetic/val/`, `data_synthetic/test_original/`, `data_synthetic/test_synthetic/`에 각각 저장 (파일명에 원본명+배경 인덱스 포함, 예: `rock_001_bg0.png`)

---

## 6. 다음 단계 — 모델 학습 (`04_model_training.ipynb`, 신규 생성)

### 6-1. 모델 구조
```
MobileNetV2 (include_top=False, weights='imagenet', trainable=False)
  → Rescaling(2.0, offset=-1.0)   ※ 아래 6-2 참고, base 입력단 앞에 위치
  → GlobalAveragePooling2D
  → Dropout
  → Dense(128, relu)
  → Dropout
  → Dense(3, softmax)
```

### 6-2. ⚠️ 확인 필요 — 정규화 범위 불일치
현재 파이프라인은 `/255.0`으로 0~1 정규화. 그러나 ImageNet 사전학습 MobileNetV2는 −1~1 입력을 전제로 학습됨. 파이프라인은 0~1로 유지하고, **모델 입력단에 `Rescaling(2.0, offset=-1.0)` 레이어를 추가**해 학습·추론 양쪽에서 자동으로 동일하게 변환되도록 처리 (ARCHITECTURE.md §5 권장안).

### 6-3. 학습 설정

| 항목 | 값 |
|---|---|
| 입력 크기 | 224×224×3 |
| 배치 크기 | 32 |
| 손실 함수 | sparse_categorical_crossentropy |
| 옵티마이저 | Adam, lr=1e-3 (1단계) |
| 1단계 | base 동결, 10 epoch — 분류 헤드만 학습 |
| 2단계(선택) | 상위 블록 unfreeze, lr=1e-5 — fine-tuning |
| 조기 종료 | val_loss, patience=5, 최적 가중치 복원 |
| 체크포인트 | save_best_only=True |

### 6-4. 평가
- 학습 곡선(accuracy/loss) 시각화 — train/val 간격으로 과적합 확인
- confusion matrix — 특히 **가위/보 혼동** 여부 중점 확인 (둘 다 손가락 편 모양이라 헷갈리기 쉬움)
- 클래스별 precision/recall
- **test_original vs test_synthetic 정확도 비교** → 배경 증강 효과 정량 확인
- 자체 웹캠 검증셋 30~50장으로 최종 실전 성능 측정 (§5-3 표 참고)

---

## 7. 웹캠 추론 파이프라인 (`src/webcam_app.py`)

1. OpenCV로 웹캠 프레임 캡처
2. 화면에 고정 ROI 박스 표시, 그 영역만 크롭
3. 전처리: BGR→RGB → 224×224 리사이즈 → /255.0 (학습과 동일 규격)
4. 모델 추론 (배치 크기 1)
5. 최근 N=7 프레임의 확률 벡터 평균으로 스무딩, 최대 확률이 임계값(0.75) 미만이면 "대기" 표시
6. 예측 안정화 후 쿨다운 로직 적용 → 제스처-액션 매핑 트리거
7. 화면 오버레이로 현재 예측 결과 표시

---

## 8. Git / 보안 정리 (병행 필요)

| 항목 | 상태 | 조치 |
|---|---|---|
| `.gitignore` | 프로젝트 루트에 없음 | `.env`, `.venv/`, `notebooks/.ipynb_checkpoints/`, `__pycache__/`, `models/*.keras` 등록 후 커밋 |
| `.env` (API 키) | untracked이지만 gitignore 미비로 실수 커밋 위험 | 위 조치 전까지 `git add .` 절대 금지 |
| `03_background_synthesis.ipynb` | 체크포인트에만 존재, 정식 저장 안 됨 | Jupyter에서 열어 즉시 저장 |
| 로컬-원격 동기화 | 로컬이 origin보다 1커밋 앞섬, 6개 파일 unstaged | 정리 후 commit & push |

---

## 9. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 배경 편향 (도메인 갭) | 배경 합성 증강(진행 중), ROI 크롭, 자체 웹캠 데이터 보강 |
| 가위/보 혼동 가능성 | confusion matrix로 사후 확인, 필요시 해당 클래스 데이터/증강 보강 |
| Unsplash API 요청 제한 | 50장으로 우선 진행, 여유 있을 때 나머지 50장 보강 |
| 정규화 범위 불일치 | 모델 입력단 `Rescaling` 레이어로 해결 예정 (§6-2) |
| API 키 노출 | `.gitignore` 정리로 원천 차단 |

---

## 10. 남은 일정 체크리스트

- [x] `.gitignore` 생성 및 git 정리(§8)
- [x] 배경 합성 파이프라인 정식 저장 (`scripts/03_background_synthesis.py`)
- [x] 크로마키 마스킹 함수 작성 및 HSV 임계값 튜닝 (H 35~85, S/V ≥ 60)
- [x] train(×2)/val(×1)/test(원본+합성) 배경 합성 이미지 생성 및 시각 검증 (`outputs/synthesis_samples.png`)
- [x] `scripts/04_train_model.py`: 모델 구축 → 1차 학습(head) → 평가 (val 0.951)
- [x] confusion matrix + test_original/test_synthetic 비교 분석 (0.839 vs 0.936)
- [x] `src/webcam_app.py` 구현 (ROI, 스무딩 N=7, 쿨다운 2s, 액션 매핑)
- [ ] 자체 웹캠 검증셋 30~50장 촬영 및 실전 테스트 (사람 필요)
- [ ] 실전 성능 미달 시 상위 블록 unfreeze fine-tuning (lr=1e-5)
- [x] README.md 진행 현황 갱신

※ 구현 노트: 노트북(.ipynb) 대신 재실행 가능한 스크립트(`scripts/01~04`)로 구현함.
※ 트러블슈팅: Windows 한글 경로에서 cv2.imread/imwrite 실패 → np.fromfile+imdecode 우회.
